//import { State } from "react-native-gesture-handler";

/*function reducerProfil(state, action){
    let nextState

    switch(action.type){
        case'ADD_PROFIL':// Ajout du profil contenu dans action.value au state de mon application
        nextState = {
            ...state,
            profil: action.value
        }
            return nextState
        case'UPDATE_PPROFIL':
            return nextState
        case'DELETE_PROFIL':

            return nextState
        default:
            return nextState 
    }
    return nextState
}
*/
